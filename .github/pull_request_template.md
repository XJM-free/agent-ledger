## What this PR does

<!-- One or two sentences. -->

## Checklist

- [ ] `bun run typecheck` passes
- [ ] `bun test` passes
- [ ] If you touched `src/pricing.ts`, there's a matching test in
      `test/pricing.test.ts`
- [ ] If you touched `src/parser.ts`, the change doesn't break the nested
      `subagents/` walk (see `test/parser.test.ts`)
- [ ] No runtime dependencies added (this tool intentionally has none)

## Log snippet (if relevant)

<!-- If the change is about handling some real log shape, include a redacted
example of what it looks like. Helps me reason about whether other tools
produce the same shape. -->
